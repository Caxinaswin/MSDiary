@model IEnumerable<MSDiary.Models.TipoDespesa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoDespesaNome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.subTipo.TipoDespesaNome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoDespesaNome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.subTipo.TipoDespesaNome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoDespesaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoDespesaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoDespesaId })
        </td>
    </tr>
}

</table>
